[package]
name = "capsula-mutirao"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
sha3 = { version = "0.10", default-features = false }
wee_alloc = { version = "0.4", default-features = false }
